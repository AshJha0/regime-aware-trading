[package]
name = "regime"
version = "1.0.0"
edition = "2021"
description = "Regime-aware trading: Gaussian HMM, momentum, FX carry, backtest (P11 port)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
rand = "0.8"
rand_distr = "0.4"
